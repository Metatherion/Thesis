<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.andtest2">

    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SpiritualActivity"
            android:label="Spirituality and Mindfullness"></activity>
        <activity android:name=".DietActivity"
            android:label="Diet and Nutrition"/>
        <activity android:name=".ActivityActivity"
            android:label="Activity and Social"/>
        <activity android:name=".BehaviourActivity"
            android:label="Behaviour and Thought"/>
        <activity android:name=".MoodActivity"
            android:label="Mood"/>
        <activity android:name=".SleepActivity"
            android:label="Sleep"/>
        <activity android:name=".ExerciseActivity"
            android:label="Exercise and Fitness"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>